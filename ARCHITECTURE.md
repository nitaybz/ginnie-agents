# Architecture

ginnie-agents is a multi-agent framework that runs each agent as an isolated Docker container, talking to humans (and to each other) through Slack via **Socket Mode** (WebSocket), with shared persistent memory and a curated team directory.

## Slack transport: Socket Mode (not webhooks)

The framework uses Slack's [Socket Mode](https://api.slack.com/apis/connections/socket) for all bot-to-Slack communication. Each agent's `@slack/bolt` App opens a persistent WebSocket connection to Slack. There are **no public webhook URLs**; the framework does not need an inbound HTTP server, reverse proxy, or static IP — it works behind NAT, on a laptop, on a home server, anywhere with outbound TCP.

Implications:
- The required Slack scopes include `connections:write` on each agent's App-Level Token (the `xapp-…` token).
- The bot's "Event Subscriptions" must be **enabled in Slack** but the **Request URL field stays empty** — events flow over the WebSocket, not to a URL.
- Each agent owns its own connection — agents do not share a Slack identity. The framework's listener spawns one Bolt app per agent so message routing is handled natively by Slack.

## Topology

```
┌─────────────────────────────────────────────────────────────┐
│                Host (Linux/macOS, PM2)                      │
│                                                             │
│  ┌─────────────────── listener (Node) ──────────────────┐   │
│  │  one @slack/bolt App per agent (Socket Mode)         │   │
│  │  scheduler — fires routines per agent's schedule     │   │
│  │  runner    — spawns Docker container per session     │   │
│  └──────────────────────────────────────────────────────┘   │
│                            │                                │
│           ┌────────────────┼────────────────┐               │
│           ▼                ▼                ▼               │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│   │  Agent A    │  │  Agent B    │  │  Agent N    │         │
│   │  Docker     │  │  Docker     │  │  Docker     │         │
│   │  container  │  │  container  │  │  container  │         │
│   │ (ephemeral) │  │ (ephemeral) │  │ (ephemeral) │         │
│   └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
            │                │                │
            └────────────────┼────────────────┘
                             ▼
                    ┌─────────────────┐
                    │   Slack         │
                    │   workspace     │
                    └─────────────────┘
```

The listener is a single long-running Node process. It auto-discovers agents under `agents/`, spins up a Slack app per agent, and spawns a fresh container for every Slack event or scheduled routine. Containers are short-lived; state lives on the host (memory files, sessions for resume).

## The 8-layer agent model

Every agent has exactly these slots, no more, no less:

| Layer | What | Location | Sharing |
|---|---|---|---|
| **Identity** | name, Slack app, bot/app tokens | `agents/<n>/credentials.json` + `slack.json` | per-agent |
| **Voice** | `SOUL.md` — backstory, voice, quirks | `agents/<n>/SOUL.md` | per-agent |
| **Mission** | `PROMPT.md` — role and behaviors | `agents/<n>/PROMPT.md` | per-agent |
| **Memory** | rules / playbook / episodes | `agents/<n>/memory/` | per-agent |
| **Skills** | personal + shared + framework | `agents/<n>/skills/` ∪ `shared/skills/` ∪ `framework/skills/` | union |
| **Network: humans** | known-users | `shared/known-users.json` ∪ `agents/<n>/known-users.json` | shared ∪ local |
| **Network: agents** | team directory | auto-generated by runner from `agents/` | runner-injected |
| **Schedule** | routines, cron triggers | `agents/<n>/schedules.json` | per-agent |

Plus operational state in `config.json`: `boundaries` (read-only / write), `work_hours`, `max_turns`, `model`, `allowed_tools`.

## System prompt composition

When the runner spawns an agent container, the entrypoint composes the system prompt by concatenating, in order:

1. **`shared/foundation.md`** (optional) — user-supplied team/company context.
2. **Team directory** — auto-rendered from merged `shared/known-users.json` ∪ `agents/<n>/known-users.json`.
3. **`SOUL.md`** — wrapped in framing that says "this is who you are; speak from it, don't quote it back."
4. **`framework/skills/memory-curation/SKILL.md`** — canonical memory model, framework-shipped.
5. **`PROMPT.md`** — agent-specific role and behaviors.
6. **`memory/rules.md`** — always-loaded user-stated rules (≤200 lines, hard cap).
7. **`memory/playbook.md`** — always-loaded settled patterns (≤300 lines, hard cap).

Episodes (`memory/episodes/YYYY-Qn.md`) are NOT injected — agents grep them on demand. This keeps the system prompt bounded regardless of how long the agent has been running.

## Memory model (three tiers + git enforcement)

| Tier | File | Cap | Loaded? | Writer |
|---|---|---|---|---|
| Rules | `rules.md` | 200 lines | Always | Agent edits in place, immediately, on first hearing the user state a requirement |
| Playbook | `playbook.md` | 300 lines | Always | Nightly consolidation routine only |
| Episodes | `episodes/YYYY-Qn.md` | — | Lazy (grep on demand) | Agent appends during sessions |

**Git enforcement layers:**
1. `.gitattributes` sets `merge=union` on `agents/*/memory/**/*.md` → any merge auto-keeps both sides' lines.
2. `scripts/hooks/commit-msg` rejects commits that exceed caps or shrink an episode file. The only legitimate shrink is a commit whose message starts with `memory-consolidate:` (the nightly cron's bypass token).

Run `scripts/hooks/install.sh` once per fresh clone to wire `core.hooksPath`.

## Network: shared ∪ local

**Skills:** every agent sees three skill directories layered together — `agents/<n>/skills/` (personal), `shared/skills/` (cross-agent user skills), `framework/skills/` (framework-shipped). Skills are additive, not override.

**Known-users:** shared ∪ local with **per-entry** override. If the same Slack ID appears in both `shared/known-users.json` and `agents/<n>/known-users.json`, the local entry wins for that key only. Never whole-file replacement.

This enables selective visibility: a customer-success agent can know specific customers without those entries leaking into infra agents' team directory. Empty agents-only files just see the shared list unchanged.

## Boundaries (read-only / write)

Each agent's `config.json` declares `"boundaries": "read-only"` or `"write"` (default). The runner enforces this by overriding `allowed_tools` at spawn time:

- `read-only` → tool set restricted to `["Read", "Grep", "Glob", "WebSearch", "WebFetch"]`. No `Write`, no `Edit`, no `Bash`.
- `write` → no override; agent gets the declared tools.

This is not a sandbox — agents still run as their declared identity in their container — but it's a hard guarantee at the SDK layer that read-only agents cannot call write tools, regardless of what their prompt says.

## Work hours

`config.json.work_hours` controls when the agent responds to inbound user messages:

```json
"work_hours": {
  "enabled": true,
  "start": "09:00",
  "end": "18:00",
  "days": ["mon", "tue", "wed", "thu", "fri"],
  "off_hours_behavior": "deferred_response"
}
```

When `enabled` is true and the current time (in the container's `TZ`) falls outside the window, the listener applies `off_hours_behavior`:

- `ignore` — silently drop the message.
- `deferred_response` (and v0.1.0 `queue`) — post a one-line off-hours notice in the same thread; do not run the agent.

Scheduled routines fire regardless of work hours — the schedule itself decides timing. Work hours only gate inbound user messages.

## Auth flow

Strongly preferred: a long-lived (~1 year) OAuth token from `claude setup-token`, exposed as `CLAUDE_CODE_OAUTH_TOKEN`. PM2 reads it from the launching shell's environment, the runner forwards it into each container, and the SDK uses it directly.

Fallback: if `CLAUDE_CODE_OAUTH_TOKEN` is not set, the runner mounts the host's `~/.claude/.credentials.json` read-only into the container. This works but only for ~8 hours — the OAuth token can't be refreshed inside a read-only mount, so daemon use will eventually fail. Use `claude setup-token` for any production deployment.

## Mount layout (inside the container)

```
/workspace/
├── PROMPT.md                       (ro, per-agent)
├── SOUL.md                         (ro, per-agent)
├── credentials.json                (ro, per-agent, gitignored on host)
├── schedules.json                  (rw — agent edits this)
├── known-users.json                (ro, per-agent — local known-users only; merged with shared by entrypoint)
├── memory/                         (rw)
│   ├── rules.md
│   ├── playbook.md
│   └── episodes/YYYY-Qn.md
├── skills/                         (ro, per-agent)
├── .shared/                        (ro)
│   ├── foundation.md               (optional)
│   ├── known-users.json
│   └── skills/                     (cross-agent user skills)
└── .framework/skills/              (ro, framework-shipped — memory-curation, etc.)
```

The agent itself only references the unprefixed paths (`./memory/`, `./skills/`, `./schedules.json`). Anything under `.shared/` or `.framework/` is auto-loaded by the entrypoint and shouldn't be read directly from `PROMPT.md`.

## Scheduler

`listener/src/scheduler.ts` watches each agent's `schedules.json` and fires routines via `node-cron`. The timezone is the listener-wide `TZ` env var (defaults to UTC). Cron expressions are interpreted in that TZ.

When a schedule fires, the listener spawns a fresh container with the schedule's `message` as the input. The agent's response is logged but typically not sent anywhere — most schedules are autonomous (e.g., "scan fleet, post if anything's wrong"). The agent itself decides whether to post to Slack.

## Update flow

Framework updates ship via `git pull`. User content lives in directories the framework never writes to (`agents/`, `shared/`, `config/`, `.env`), so pulls don't conflict with user state. The `update-framework` skill wraps the predictable sequence:

```bash
git pull
# if docker/ or framework/skills/ changed: rebuild image
docker build -t ginnie-agent -f docker/Dockerfile .
cd listener && npm install && npm run build
pm2 restart ecosystem.config.cjs --update-env
# run doctor
```

## What's deliberately NOT in this framework

- **Multi-LLM**: Claude Code + Max only. No OpenAI, no Gemini, no API SDK.
- **Non-Slack platforms**: no Discord, no Teams, no email integrations.
- **Cost/budget management**: Max is flat-rate; nothing to budget.
- **Host-Claude (non-Docker) execution mode**: agents always run isolated.
- **Hosted/SaaS version**: this is self-hosted only.
- **Web UI / dashboard**: Slack is the interface.
- **Multi-machine clustering**: single-host.
- **Windows host**: the framework has been validated on macOS and Linux only.
